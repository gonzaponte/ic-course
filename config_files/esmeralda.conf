event_range = all
compression = "ZLIB4"
print_mod   = 1

detector_db = "new"
run_number = -6400

threshold = 15 * pes
same_peak = True

fiducial_r = 200 * mm

paolina_params      = dict(
   vox_size         = [10 * mm, 10 * mm, 10 * mm],
   strict_vox_size  = True,
   energy_threshold = 10 * keV,
   min_voxels       = 4,
   blob_radius      = 21 * mm,
   max_num_hits     = 10000)